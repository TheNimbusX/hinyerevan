# Base URL of the backend API.
VITE_API_URL=http://127.0.0.1:8000/api
# Public site URL for Open Graph tags in index.html (no trailing slash).
VITE_SITE_URL=https://hinyerevan.com
# Dev stand: set true on hinyerevan.ru builds (see deploy/deploy-dev.sh).
VITE_DEV_AUTH_REQUIRED=
# Yandex Maps JS API key (Before/After panoramas).
VITE_YANDEX_MAPS_KEY=
# Google Maps JS API key with Street View enabled (Before/After panoramas).
VITE_GOOGLE_MAPS_KEY=
